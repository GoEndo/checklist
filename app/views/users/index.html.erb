<% provide(:title, 'All users') %>

<h1>Users</h1>

<%= link_to 'New User', new_user_path %><br>

<p></p>

<%= will_paginate %>

<table class="listing">
  <tr>
    <th></th>
    <th>Name</th>
    <th>E-mail</th>
    <th>Role</th>
    <th>Activation</th>
  </tr>
  <%= render @users %>
</table>

<%= will_paginate %>

<%= link_to 'Back', root_path %>